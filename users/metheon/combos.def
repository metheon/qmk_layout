#include "metheon.h"

/*                KEY POSITIONS
  ╭────────────────────╮ ╭────────────────────╮
  │  j   g   m   p   b │ │  `   u   o   y   @ │
  │  c   s   n   t   w │ │  '   a   e   i   h │
  │  x   f   l   d   v │ │  -   k   ,   .   / │
  ╰───────────╮ En  Sp │ │  r  Bs ╭───────────╯
              ╰────────╯ ╰────────╯            
*/

COMB(COMBO_Q             ,KC_Q       ,KC_G, KC_M)            // Q
COMB(COMBO_Z             ,KC_Z       ,KC_F, KC_L)            // Z

COMB(COMBO_AE            ,KC_AE      ,HOME_A, HOME_E)        // Æ
COMB(COMBO_OE            ,KC_OE      ,HOME_E, HOME_I)        // Ø
COMB(COMBO_AA            ,KC_AA      ,HOME_A, HOME_I)        // Å

COMB(COMBO_QUES          ,KC_QUES    ,HOME_U, KC_Y)          // ?
COMB(COMBO_EXLM          ,KC_EXLM    ,KC_COMMA, KC_QUOT)     // !

//COMB(COMBO_RSFT2         ,SHIFT      ,HOME_A, HOME_E)        // one shot shift
//COMB(COMBO_LSFT2         ,SHIFT      ,HOME_N, HOME_T)        // one shot shift

COMB(COMBO_PASTE           ,PASTE      ,CUT, COPY)           // undo
